"""Sunday's solar physics sidecar.

Wraps pvlib so the desktop app gets peer-reviewed PV models instead of
hand-rolled approximations. Every response names the models it used.
"""

__version__ = "0.1.0"
